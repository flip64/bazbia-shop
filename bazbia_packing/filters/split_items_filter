# bazbia_packing/filters/split_items_filter.py

class SplitItemsFilter:
    def filter(self, boxes, items):
        """
        فیلتر تقسیم‌بندی — نسخه فعلی:
        فقط ورودی را بدون هیچ تغییری برمی‌گرداند.
        منطق واقعی بعداً اضافه می‌شود.
        """
        return boxes, items
